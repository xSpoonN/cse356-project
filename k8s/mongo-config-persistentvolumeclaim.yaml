apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: mongo-config
  name: mongo-config
spec:
  storageClassName: upcloud-block-storage-maxiops
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
