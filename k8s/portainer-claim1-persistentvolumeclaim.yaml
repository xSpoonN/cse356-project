apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: portainer-claim1
  name: portainer-claim1
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
