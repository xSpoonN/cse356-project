apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: fluentd-claim0
  name: fluentd-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
