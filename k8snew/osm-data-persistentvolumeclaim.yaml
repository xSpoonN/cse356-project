apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: osm-data
  name: osm-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
