apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: portainer-data
  name: portainer-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
